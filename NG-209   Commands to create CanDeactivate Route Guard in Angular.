<h1>
    Angular 19 Course by Tech Sharmit
</h1>

<h2>
    Commands to create CanDeactivate Route Guard in Angular.
</h2>


<p>
    Commands :-

    Command-1 : ng generate guard FolderName/RouteGuardName --implements CanDeactivate

    Command-2 : ng g guard FolderName/RouteGuardName --implements CanDeactivate

    Command-3 : ng g g FolderName/RouteGuardName --implements CanDeactivate
</p>
