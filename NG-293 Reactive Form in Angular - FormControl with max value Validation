<h1>
    Angular Course by Tech Sharmit
</h1>

<h1>
    Reactive Form in Angular - FormControl with max Value Validation
</h1>

<hr>

<form>

    <label>
        Age :
    </label>

    <input type="number" [formControl]='Age'>

    <p *ngIf="Age.errors?.['max']">
        Age should be lesser then 18.
    </p>


</form>



<hr>

import { NgIf } from '@angular/common';
import { Component } from '@angular/core';
import { FormControl, ReactiveFormsModule, Validators } from '@angular/forms';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [ReactiveFormsModule, NgIf],
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css'],
})
export class AppComponent {

  Age = new FormControl('', Validators.max(18));

}
