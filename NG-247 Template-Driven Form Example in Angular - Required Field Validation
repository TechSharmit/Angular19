<h1>
    Angular Course by Tech Sharmit
</h1>

<h1>
    Template-Driven Form Example in Angular - Required Field Validation
</h1>

<hr>

<form #MyForm="ngForm" (ngSubmit)="onSubmit(MyForm)">

    <label>
        Username :
    </label>


    <input type="text" name="Username" ngModel required #Username="ngModel">

    <!-- Error Message -->

    <div *ngIf="Username.invalid && Username.touched" style="color: red;">

        <p>
            Username is required.
        </p>
    </div>

    <button type="submit" [disabled]="MyForm.invalid">Submit</button>

</form>


import { NgIf } from '@angular/common';
import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [FormsModule, NgIf],
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css'],
})
export class AppComponent {
  onSubmit(Form: any) {
    console.log('Form Vaues', Form.value);
    alert('Form Submitted');
  }
}
